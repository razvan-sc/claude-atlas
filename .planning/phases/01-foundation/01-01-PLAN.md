---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/atlas/__init__.py
  - src/atlas/config.py
  - src/atlas/graphql_client.py
  - src/atlas/server.py
  - tests/test_config.py
  - tests/test_graphql_client.py
  - tests/__init__.py
  - .mcp.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Server reads email, API token, and subdomain from ~/.atlas/config.json"
    - "MCP server starts on stdio transport and Claude Code discovers its tools"
    - "Server can execute a raw GraphQL query against the Atlas API and return a result"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependencies"
      contains: "mcp"
    - path: "src/atlas/config.py"
      provides: "Config file loader and validation"
      exports: ["load_config", "AtlasConfig"]
    - path: "src/atlas/graphql_client.py"
      provides: "GraphQL client with Basic Auth"
      exports: ["AtlasGraphQLClient"]
    - path: "src/atlas/server.py"
      provides: "MCP server with tool registration"
      contains: "FastMCP"
    - path: ".mcp.json"
      provides: "Claude Code MCP server discovery"
      contains: "mcpServers"
  key_links:
    - from: "src/atlas/server.py"
      to: "src/atlas/config.py"
      via: "import load_config"
      pattern: "from atlas\\.config import"
    - from: "src/atlas/server.py"
      to: "src/atlas/graphql_client.py"
      via: "import AtlasGraphQLClient"
      pattern: "from atlas\\.graphql_client import"
    - from: "src/atlas/graphql_client.py"
      to: "https://{subdomain}.atlassian.net/gateway/api/graphql"
      via: "httpx POST with Basic Auth header"
      pattern: "httpx.*post|Authorization.*Basic"
---

<objective>
Stand up the complete MCP server foundation: config loading, GraphQL client, and tool registration.

Purpose: Deliver a working MCP server that Claude Code can discover, that authenticates against the Atlas API, and exposes a raw GraphQL query tool.
Output: Runnable Python MCP server with config, GraphQL client, and Claude Code integration.
</objective>

<execution_context>
@/Users/razvanbalazs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/razvanbalazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold and config loader</name>
  <files>pyproject.toml, src/atlas/__init__.py, src/atlas/config.py, tests/__init__.py, tests/test_config.py</files>
  <behavior>
    - load_config() reads ~/.atlas/config.json and returns AtlasConfig with email, api_token, subdomain
    - load_config() raises FileNotFoundError with helpful message when config file missing
    - load_config() raises ValueError when required fields (email, api_token, subdomain) are missing
    - AtlasConfig.base_url property returns "https://{subdomain}.atlassian.net/gateway/api/graphql"
    - AtlasConfig.auth_header property returns "Basic {base64(email:api_token)}"
  </behavior>
  <action>
    1. Create pyproject.toml:
       - name: "claude-atlas"
       - requires-python: ">=3.10"
       - dependencies: ["mcp>=1.0", "httpx>=0.27"]
       - dev dependencies: ["pytest>=8.0", "pytest-asyncio>=0.24"]
       - entry point: atlas = "atlas.server:main"
       - src layout (packages = [{include = "atlas", from = "src"}])

    2. Create src/atlas/__init__.py (empty) and tests/__init__.py (empty)

    3. Create src/atlas/config.py:
       - dataclass AtlasConfig with fields: email (str), api_token (str), subdomain (str)
       - Property base_url: returns f"https://{self.subdomain}.atlassian.net/gateway/api/graphql"
       - Property auth_header: returns f"Basic {base64.b64encode(f'{self.email}:{self.api_token}'.encode()).decode()}"
       - Function load_config(path: Path | None = None) -> AtlasConfig:
         - Default path: Path.home() / ".atlas" / "config.json"
         - Read JSON, validate required fields present and non-empty
         - Return AtlasConfig instance
         - FileNotFoundError if file missing (message: "Config file not found at {path}. Create it with: {\"email\": \"...\", \"api_token\": \"...\", \"subdomain\": \"...\"}")
         - ValueError if fields missing

    4. Create tests/test_config.py with tests for all behaviors above using tmp_path fixture for config files.

    5. Run RED then GREEN: write tests first, confirm they fail, then implement.
  </action>
  <verify>
    <automated>cd /Users/razvanbalazs/Work/Claude/claude-atlas && python -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <done>AtlasConfig loads and validates config, all tests pass, pyproject.toml has correct dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GraphQL client with Basic Auth</name>
  <files>src/atlas/graphql_client.py, tests/test_graphql_client.py</files>
  <behavior>
    - AtlasGraphQLClient.__init__ accepts AtlasConfig and creates httpx.AsyncClient with auth header
    - execute(query: str, variables: dict | None) sends POST to config.base_url with {"query": query, "variables": variables}
    - execute returns the parsed JSON response dict
    - execute raises an exception when the HTTP response is not 2xx
    - execute raises an exception when GraphQL response contains "errors" key
  </behavior>
  <action>
    1. Create src/atlas/graphql_client.py:
       - Class AtlasGraphQLClient:
         - __init__(self, config: AtlasConfig): stores config, creates httpx.AsyncClient with headers={"Authorization": config.auth_header, "Content-Type": "application/json"}
         - async execute(self, query: str, variables: dict | None = None) -> dict:
           - POST to config.base_url with json={"query": query, "variables": variables or {}}
           - response.raise_for_status()
           - Parse JSON, check for "errors" key, raise RuntimeError with error details if present
           - Return full response dict (caller decides what to extract)
         - async close(self): close the httpx client
         - async __aenter__ / __aexit__ for context manager usage

    2. Create tests/test_graphql_client.py:
       - Use httpx MockTransport or unittest.mock to mock HTTP responses
       - Test successful query returns parsed JSON
       - Test HTTP error raises httpx.HTTPStatusError
       - Test GraphQL errors in response raises RuntimeError
       - Test auth header is correctly set on requests

    3. Run RED then GREEN.
  </action>
  <verify>
    <automated>cd /Users/razvanbalazs/Work/Claude/claude-atlas && python -m pytest tests/test_graphql_client.py -x -v</automated>
  </verify>
  <done>GraphQL client sends authenticated requests, handles errors, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: MCP server with tool registration and Claude Code config</name>
  <files>src/atlas/server.py, .mcp.json</files>
  <action>
    1. Create src/atlas/server.py:
       - Import FastMCP from mcp.server.fastmcp
       - Import load_config from atlas.config
       - Import AtlasGraphQLClient from atlas.graphql_client
       - Create FastMCP instance: mcp = FastMCP("atlas")
       - Register tool with @mcp.tool() decorator:

         @mcp.tool()
         async def atlas_graphql_query(query: str, variables: str = "{}") -> str:
             """Execute a raw GraphQL query against the Atlassian Atlas API.

             Args:
                 query: The GraphQL query string to execute
                 variables: JSON string of variables for the query (default: "{}")

             Returns:
                 The JSON response from the Atlas API
             """
             - Load config via load_config()
             - Parse variables from JSON string to dict
             - Create AtlasGraphQLClient(config)
             - Execute query, return json.dumps(result, indent=2)
             - Wrap in try/except, return error message string on failure (MCP tools should not raise)

       - def main(): mcp.run() — this is the entry point
       - if __name__ == "__main__": main()

    2. Create .mcp.json at project root:
       {
         "mcpServers": {
           "atlas": {
             "command": "uv",
             "args": ["run", "--directory", "/Users/razvanbalazs/Work/Claude/claude-atlas", "python", "-m", "atlas.server"]
           }
         }
       }
       Note: Use absolute path so Claude Code can find it from any working directory.

    3. Install dependencies: run `uv sync` to create venv and install all deps.

    4. Verify server starts: run `echo '{}' | timeout 3 uv run python -m atlas.server 2>&1 || true` — should not crash on import (will timeout waiting for stdio input, which is expected).
  </action>
  <verify>
    <automated>cd /Users/razvanbalazs/Work/Claude/claude-atlas && uv sync && python -c "from atlas.server import mcp; print('Server imports OK')" && python -c "from atlas.config import load_config, AtlasConfig; print('Config imports OK')" && python -c "from atlas.graphql_client import AtlasGraphQLClient; print('Client imports OK')" && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>MCP server starts without errors, registers atlas_graphql_query tool, .mcp.json exists for Claude Code discovery, all project tests pass</done>
</task>

</tasks>

<verification>
1. All tests pass: `python -m pytest tests/ -x -v`
2. Server module imports cleanly: `python -c "from atlas.server import mcp"`
3. Config loader works with valid config: create a test config and load it
4. `.mcp.json` exists at project root with correct server configuration
5. Tool is registered: `python -c "from atlas.server import mcp; print(mcp._tool_manager._tools.keys())"`
</verification>

<success_criteria>
- pyproject.toml defines claude-atlas with mcp and httpx dependencies
- Config loader reads ~/.atlas/config.json and produces auth headers
- GraphQL client sends authenticated POST requests to Atlas API
- MCP server starts and exposes atlas_graphql_query tool
- Claude Code can discover the server via .mcp.json
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
